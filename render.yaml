services:
  - type: web
    name: ghost-ai-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: DEEPSEEK_KEY
        value: your-key-here
      - key: OPENAI_KEY
        value: your-key-here